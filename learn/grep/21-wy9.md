---
title: Match exactly n times
concepts: [algorithms]
---

## What this stage teaches

`a{3}` generalises the trio: `+` is `{1,}`, `?` is `{0,1}`, `*` is `{0,}`. Once
the matcher takes a min and a max, the three shorthands become parse-time
sugar over one implementation.

## Go you'll reach for

- `min`/`max` fields on the element; a `{n}` parser.

## Hints

<details><summary>Nudge</summary>

Exactly n means min and max are both n.
</details>

<details><summary>Approach</summary>

Rewrite `+`, `?` and `*` in terms of the bounds now — three special cases
become one.
</details>

<details><summary>The API</summary>

Parse `{n}` into `min = max = n`, then rewrite `+` as `{1,}`, `?` as
`{0,1}` and `*` as `{0,}` at parse time. One matcher, four spellings.
</details>

## Going deeper

- [Interval quantifiers](https://www.regular-expressions.info/repeat.html)
