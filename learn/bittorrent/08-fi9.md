---
title: Discover peers
concepts: [networking, replication, binary formats]
---

## Core concept

The tracker is a plain HTTP endpoint, and two encoding details bite. The
`info_hash` parameter is the **20 raw bytes**, percent-encoded — not the hex
string, which is twice as long and matches nothing. And with `compact=1` the
peer list comes back as 6 bytes each: four for the IPv4 address, two for a
big-endian port.

## Go APIs

- `net/url` for the query; `url.QueryEscape` on the raw bytes.
- A loop over the response's peers string, 6 bytes at a time.

## Hints

<details><summary>Nudge</summary>

`peer_id` is any 20 bytes you choose, and `left` is the number of bytes you
still need.
</details>

<details><summary>Approach</summary>

Percent-encode the raw 20 bytes of the info hash, not its hex form — the hex
string is twice as long and matches nothing.
</details>

## Further reading

- [BEP 3 — trackers](https://www.bittorrent.org/beps/bep_0003.html#trackers)
- [BEP 23 — compact peer lists](https://www.bittorrent.org/beps/bep_0023.html)
