---
title: List for multiple topics
concepts: [binary formats]
---

## Core concept

One request, several topics, and the response keeps them in the **order asked**
— each with its own error code, so an unknown topic sits beside a known one
rather than failing the whole request. Partial success is normal in a batched
protocol.

## Go APIs

- A loop over the requested topics, emitting one response block each.

## Hints

<details><summary>Nudge</summary>

The reply's topic count always equals the request's.
</details>

<details><summary>Approach</summary>

Loop over the topics the request named, not over the topics you know. The
reply must have one block per requested topic, in that order.
</details>
<details><summary>Implementation</summary>

An unknown topic still gets a full block: its name, a zero UUID, error 3,
and an empty partition array.
</details>

## Further reading

- [Kafka protocol — DescribeTopicPartitions](https://kafka.apache.org/protocol.html#The_Messages_DescribeTopicPartitions)
