---
title: Invalid usages of 'this'
concepts: [errors, state management, replication]
---

## What this stage teaches

`this` outside a method is a static error, caught by the resolver's class
context — the same mechanism that caught top-level `return`. The resolver now
tracks two contexts, function and class, and each one guards a keyword.

## Go you'll reach for

- A `currentClass` enum alongside `currentFunction`.

## Hints

<details><summary>Nudge</summary>

`Can't use 'this' outside of a class.`
</details>

<details><summary>Approach</summary>

Track a class context on the resolver alongside the function one. Each
context guards one keyword.
</details>

## Going deeper

- [Crafting Interpreters — invalid uses of this](https://craftinginterpreters.com/classes.html#this)
