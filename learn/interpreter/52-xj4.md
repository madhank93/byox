---
title: Nested if statements
concepts: [recursion, parsing, evaluation]
---

## Core concept

An `if` inside an `if` inside a block. Nothing new is required, and that is the
observation worth keeping: a recursive grammar composes without special cases,
so "nested" is never a feature you implement — it is what you get for free by
recursing.

## Go APIs

- Nothing new.

## Hints

<details><summary>Nudge</summary>

If this needs code, look for a place where you parsed a block instead of
calling `statement()`.
</details>

<details><summary>Approach</summary>

If this needs code, look for a place where a block was required instead of
calling `statement()`.
</details>

## Further reading

- [Crafting Interpreters — Control Flow](https://craftinginterpreters.com/control-flow.html)
