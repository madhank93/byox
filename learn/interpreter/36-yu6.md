---
title: "Runtime Errors: Binary Operators (1/2)"
concepts: [errors]
---

## Core concept

The same check for `*` and `/`, with a plural message — `Operands must be
numbers.` Two operands, one message, and the check happens after both are
evaluated, because Lox evaluates both before deciding the operation is invalid.

## Go APIs

- A `checkNumberOperands(op, left, right)` helper.

## Hints

<details><summary>Nudge</summary>

Note the plural: `Operands`, not `Operand`.
</details>

<details><summary>Approach</summary>

One `checkNumberOperands` helper called by every arithmetic case keeps the
message identical everywhere it is raised.
</details>

## Further reading

- [Crafting Interpreters — runtime errors](https://craftinginterpreters.com/evaluating-expressions.html#runtime-errors)
