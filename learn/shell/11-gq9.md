---
title: "The cd builtin: Relative paths"
concepts: [relative paths, kernel-side resolution]
---

## What this stage teaches

`./dir`, `../dir` and bare `dir` are resolved **relative to the current working
directory** — and the resolution happens in the kernel, not in your shell.
`chdir` takes any path and interprets it against the process's current
directory, so relative paths need no special handling.

## Go you'll reach for

- The same `os.Chdir`.

## Hints

<details><summary>Nudge</summary>

If absolute paths already work, this likely does too — the syscall does not
distinguish them.
</details>

## Going deeper

- [`os.Chdir`](https://pkg.go.dev/os#Chdir)
