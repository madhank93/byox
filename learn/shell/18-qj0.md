---
title: Executing a quoted executable
concepts: [process control]
---

## Core concept

Quoting applies to the **command name**, not just its arguments — so
`'exe with space'` resolves to a file whose name contains spaces. This is the
payoff for removing quotes during tokenization rather than at use: by the time
lookup happens, the word is already the real filename.

## Go APIs

- The existing PATH lookup, unchanged.

## Hints

<details><summary>Nudge</summary>

If quote removal happens in the tokenizer, this stage needs nothing new.
</details>

<details><summary>Approach</summary>

Quote removal happens in the tokenizer, so by lookup time the word is
already the real filename — spaces included.
</details>

## Further reading

- [`exec.LookPath`](https://pkg.go.dev/os/exec#LookPath)
